<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/registerTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create an Account"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp" />

    <!-- Name Field -->
    <EditText
        android:id="@+id/nameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Name"
        android:inputType="textPersonName"
        android:padding="16dp"
    android:layout_below="@id/registerTitle"
    android:layout_marginBottom="12dp"/>

    <!-- Email Field -->
    <EditText
        android:id="@+id/emailEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:padding="16dp"
    android:layout_below="@id/nameEditText"
    android:layout_marginBottom="12dp"/>

    <!-- Password Field -->
    <EditText
        android:id="@+id/passwordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:inputType="textPassword"
        android:padding="16dp"
    android:layout_below="@id/emailEditText"
    android:layout_marginBottom="12dp"/>

    <Button
        android:id="@+id/requestApprovalButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Request Approval"
        android:visibility="gone"/> <!-- Initially hidden -->


    <!-- Key Field (Visible only for Event Manager) -->
    <EditText
        android:id="@+id/keyEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Key (Required for Event Managers)"
        android:inputType="text"
        android:padding="16dp"
    android:layout_below="@id/passwordEditText"
    android:layout_marginBottom="12dp"
    android:visibility="gone"/>

    <!-- Event Manager Checkbox -->
    <CheckBox
        android:id="@+id/eventManagerCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Register as Event Manager"
        android:layout_below="@id/keyEditText"
        android:layout_marginTop="12dp"/>

    <!-- Register Button -->
    <Button
        android:id="@+id/registerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Register"
        android:layout_below="@id/eventManagerCheckBox"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:padding="12dp"/>



    <!-- Sign In Button -->
    <Button
        android:id="@+id/signInButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Already have an account? Sign In"
        android:layout_below="@id/registerButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:padding="12dp"/>



</RelativeLayout>
